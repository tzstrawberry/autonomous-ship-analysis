[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bka"
version = "0.1.0"
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "pyarrow"
]

[tool.setuptools.packages.find]
where = ["src"]